# 9router — agent notes

## Two builds live in this repo (don't confuse them)
- **`.next/standalone/`** — plain `next build` output. Run directly:
  `PORT=20128 HOSTNAME=127.0.0.1 node .next/standalone/server.js`
- **`cli/app/`** — the build the **bare `9router` command runs** (global bin is a
  symlink → `~/tools/9router/cli`). Bundled via `cli/scripts/build-cli.js`, uses
  `custom-server.js` (injects real socket IP), binds `0.0.0.0` by default
  (LAN-exposed — use `9router -H 127.0.0.1` for local-only), interactive menu,
  `--max-old-space-size=6144`, detached, auto-restart.

**They are NOT the same.** A source edit (e.g. `src/lib/serviceManager.js`) only
reaches bare `9router` after rebuilding `cli/app`:

```bash
npm --prefix ~/tools/9router/cli run build   # runs next build + repacks cli/app
```

Runs `next build` then copies `.next/standalone` → `cli/app`. Do this after ANY
source change you want the short `9router` command to pick up.

## What persists vs. what's build-specific
- **SQLite DB** (`%APPDATA%\9router\db\data.sqlite`) is shared across both builds —
  upstream providers (e.g. `morph/*`) survive regardless of which build runs.
- **Managed services** (services page) are compiled from `serviceManager.js`
  `DEFAULT_SERVICES` into whichever build runs → rebuild `cli/app` to carry them.
  `loadConfig()` only patches EXISTING default ids; a NEW service must be added to
  `DEFAULT_SERVICES` source + rebuilt.

## morph-proxy integration (friend's school project)
- Upstream provider `morph/*` (OpenAI-compatible) → registered in SQLite via REST
  API; proxy at `C:/Tools/morph-proxy-py` (FastAPI, :8790).
- Managed service `morph-proxy` in `DEFAULT_SERVICES` (category Proxy, port 8790,
  `python -m uvicorn app:app`); needs the proxy's `/pool-status` health endpoint.
- Full detail: `C:/Tools/morph-proxy-py/.agents/MEMORY.md`.

## EADDRINUSE on `9router` (port 20128 already in use) — FIXED
Symptom: `Error: listen EADDRINUSE ... 127.0.0.1:20128`.
Cause: the CLI's `killProcessOnPort` (cli.js) killed only the FIRST listening PID
then waited a fixed 500ms — too short on Windows (socket lingers) and it missed
foreign listeners (e.g. a manually-started `node .next/standalone/server.js`).
Fix applied in `cli/cli.js`: kill EVERY listening PID on the port, then POLL up to
6s until the port is actually free before spawning. (cli.js is NOT regenerated by
`build-cli.js`, so this patch survives `cli/app` rebuilds.)

Manual escape hatch if it ever recurs:
```powershell
Get-NetTCPConnection -LocalPort 20128 -State Listen | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

## Admin API quick-ref (login pw `123456`)
- Services: `POST /api/v1/admin/services {action:"start|stop",service:"<id>"}`
- CLI: `9router --svc-start <id>` / `--svc-stop` / `--svc-status`
- Health: `GET :20128/api/health`
